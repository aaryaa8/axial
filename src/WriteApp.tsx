import ComingSoon from "./components/ComingSoon";

export default function WriteApp() {
  return (
    <ComingSoon
      name="Axial Write"
      line="Letter formation through to sentences, reading from the same profile the reading tutor already holds."
    />
  );
}
